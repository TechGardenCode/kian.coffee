// Framework-agnostic TS lives here — no Angular imports.
// `defineVariants()` and `behaviors/` (clipboard, scroll-spy, mode toggle,
// slider bubble, tab roving, tooltip) land here alongside the first component.
export * from "./variants";
export * from "./behaviors";
